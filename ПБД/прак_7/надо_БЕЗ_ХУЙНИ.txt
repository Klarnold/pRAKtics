1) у всех первичных ключей должен быть serial или big serial
2) у всех не первичных ключей не может быть serial или big serial (заменить все bigserial на bigint, а потом все serial на integer)
3) заменить все varchar(500) на меньшие значения (например, varchar(50))
------------
вывести наполнение одной вместительной таблицы
1) 5 скриншотов
	а) скриншот фрагмента исходного запроса на добавление таблицы
	б) скриншот куска запроса на добавление данных в таблицы (любой в любую)
	в) скриншот запроса select * from table
	г) скриншот со списком таблиц в левой панели(делать в Dbevere)
	д) надо перейти в таблицы, а после в grdiagramm
2) postgres 5432
установить только дебивер
postgres устанавливать не обязательно
делать чётко
делаем
	1) создать новое соединение
	2) выбрать слоника
	3) выделяем хоста с сдо и вставляем в хост
	4) dbstud - база данных. Порт - 5432
	5) данные доступа к субд в СДО. Находим по номеру студенту себя. И вставляем логин и пароль
	6) заходим в схемы и находим себя по логину
	7) нажимаем правой кнопки мыши по себе, новый редактор скл, редактор скл
	8) в него вставляем код из chart db
	9) нажать оранжевый свиток - начать скрипт
если была ошибка - первичный ключ ссылается на внешний, то нужно поменять связь.
если пишет, что доступ закрыт, то возможно было подключение не к той базе данных
БЕЗ КИРИЛИЦЫ